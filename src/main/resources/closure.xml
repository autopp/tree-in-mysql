<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tree.ClosureDivisionMapper">
  <insert id="addDivision" parameterType="java.lang.Integer">
    INSERT INTO division_closure (id) VALUES (#{id});
  </insert>
  <insert id="addDivisionPath" parameterType="tree.ClosureDivisionRepository$Division">
    INSERT INTO division_closure_path (ancestor, descendant)
      SELECT other.ancestor, #{id} FROM division_closure_path AS other
        WHERE other.descendant = #{parentId}
      UNION ALL
      SELECT #{id}, #{id};
  </insert>
  <select id="getAncestorsOf" resultType="java.lang.Integer" parameterType="java.lang.Integer">
    SELECT ancestor FROM division_closure_path
      WHERE descendant = #{id} AND ancestor != #{id}
  </select>
  <select id="getDescendantsOf" resultType="java.lang.Integer" parameterType="java.lang.Integer">
    SELECT descendant FROM division_closure_path
      WHERE ancestor = #{id} AND descendant != #{id};
  </select>
</mapper>
